<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <!-- Header Section -->
    <header class="bg-white shadow-md p-4">
        <h1 class="text-2xl font-bold text-gray-800">Delete Product</h1>
        <nav class="mt-2">
            <a href="/" class="text-blue-600 hover:text-blue-800 mr-4">Home</a>
            <a href="/product" class="text-blue-600 hover:text-blue-800">View Products</a>
        </nav>
    </header>

    <!-- Main Content Section -->
    <main class="mt-8 p-4 max-w-3xl mx-auto bg-white rounded-lg shadow-md">
        <!-- Product Information -->
        <h2 class="text-xl font-semibold text-gray-700">Are you sure you want to delete this product?</h2>
        <div class="mt-4">
            <p class="text-gray-600">
                <strong>Product Name:</strong> <%= product.name %><br>
                <strong>Brand:</strong> <%= product.brand %><br>
                <strong>Price:</strong> $<%= product.price %><br>
                <strong>Category:</strong> <%= product.category %>
            </p>
        </div>

        <!-- Action Buttons -->
        <div class="mt-6 flex space-x-4">
            <!-- Confirm Deletion -->
            <form action="/delete/<%= product._id %>?_method=POST" method="POST">
                <button type="submit"
                        class="w-full bg-red-600 text-white py-2 px-4 rounded-md shadow hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500">
                    Yes, Delete
                </button>
            </form>

            <!-- Cancel Button -->
            <a href="/product" class="w-full bg-gray-300 text-gray-800 py-2 px-4 rounded-md shadow hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500">
                Cancel
            </a>
        </div>
    </main>
</body>
</html>
